node_modules/
dist/
.DS_Store

# One-off snapshots a file writer leaves beside the file it rewrote, e.g.
# client.js~RF1cb3f49b.TMP. Never part of the bundle; they must not reach
# git status or get published with the package.
*~[0-9A-Za-z]*.TMP
